source(file.path(Sys.getenv("HOME"), ".Rprofile"))

ggplot2::theme_set(
  hrbrthemes::theme_ipsum_rc(
    axis_title_size = 12,
    base_size = 12,
    grid = "Y"
  )
)

options(
  defaultPackages = c("base", "stats", "graphics", "nanoparquet", "readxl", "tidyverse")
)

scale_y_percent <- function(...) {
  scale_y_continuous(..., labels = scales::label_percent())
}
